# MOUSE-TRAP

**MOUSE_TRAP** GUI built with PyQt6.

This repo uses a modern **src/** layout and standard Python packaging so you can install it with:

```bash
pip install -e .
```

Then run the app from anywhere:

```bash
mouse-trap
```

See `docs/` for Sphinx documentation.
